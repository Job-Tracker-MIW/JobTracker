{
  "cacheDirectories": [
    "client/node_modules"
  ],
  "scripts": {
    "heroku-postbuild": "cd client && npm install && npm run build && cd .."
  },
  "dependencies": {
    "bootstrap": "^5.1.3",
    "react-router-dom": "^5.3.0"
  }
}
